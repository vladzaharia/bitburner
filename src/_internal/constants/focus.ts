/** Top priority task. */
export const TOP_PRIORITY = 0;

/** High priority task. */
export const HIGH_PRIORITY = 5;

/** Default priority task. */
export const MEDIUM_PRIORITY = 100;

/** Low priority task. */
export const LOW_PRIORITY = 1000;

/** Lowest priority task. */
export const LOWEST_PRIORITY = Infinity;

/** Default sleep time, 15 minutes. */
export const DEFAULT_SLEEP_TIME = 15 * 60 * 1000;

/** Default check interval for soft-sleep, 1 minute. */
export const DEFAULT_CHECK_INTERVAL = 60 * 1000;
